# # 目前只是随手写的估计都不对，后面改
# # 下载 go-task: https://github.com/go-task/task/releases
# # 为啥用 go-task?: 这玩意可比 make 好多了感觉...

# version: '3'

# env:
#   HARBOR: harbor.aityp.com
#   PROJECT: myproject
#   VERSION:
#     sh: git describe --tags --always --dirty 2>/dev/null || echo "dev-$(date +%Y%m%d-%H%M%S)"

# tasks:
#   # 默认任务：编译所有服务
#   default:
#     deps: [build-all]

#   # 编译所有
#   build-all:
#     deps: [build-api, build-consumer]

#   # 编译 user-api（自动处理 Windows/Linux）
#   build-api:
#     internal: true
#     cmds:
#       - |
#         cd user-api
#         GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build \
#           -ldflags="-s -w" \
#           -o bin/user-api .
#     generates:
#       - user-api/bin/user-api

#   # 编译 user-consumer
#   build-consumer:
#     internal: true
#     cmds:
#       - |
#         cd user-consumer
#         GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build \
#           -ldflags="-s -w" \
#           -o bin/user-consumer .
#     generates:
#       - user-consumer/bin/user-consumer

#   # 打包镜像（依赖编译产物）
#   docker-build:
#     deps: [build-all]
#     cmds:
#       - docker build -t {{.HARBOR}}/{{.PROJECT}}/user-api:{{.VERSION}} -f user-api/Dockerfile user-api/
#       - docker build -t {{.HARBOR}}/{{.PROJECT}}/user-consumer:{{.VERSION}} -f user-consumer/Dockerfile user-consumer/
#       - echo "Built version: {{.VERSION}}

#   # 推送镜像
#   docker-push:
#     deps: [docker-build]
#     cmds:
#       - docker push {{.HARBOR}}/{{.PROJECT}}/user-api:{{.VERSION}}
#       - docker push {{.HARBOR}}/{{.PROJECT}}/user-consumer:{{.VERSION}}

#   # 本地部署（docker-compose 替换版本号）
#   deploy-local:
#     deps: [docker-build]
#     cmds:
#       - export API_VERSION={{.VERSION}} && export CONSUMER_VERSION={{.VERSION}} && docker compose up -d

#   # 一条龙：编译 → 打包 → 推送 → 部署
#   release:
#     deps: [docker-push, deploy-local]
#     cmds:
#       - echo "Released {{.VERSION}}"

#   # 查看当前版本
#   version:
#     cmds:
#       - echo "Current version: {{.VERSION}}